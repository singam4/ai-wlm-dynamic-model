# AI WLM Dynamic Model

Small illustrative simulator for discussing dynamic load limiting trade-offs.

The model compares:
- fixed capacity with no dynamic load limiting;
- reactive dynamic load limiting;
- AI-assisted pre-warm.

This is a discussion aid only. The constants are synthetic and are not production thresholds. Any production rollout must be calibrated from DLL telemetry and shadow-mode outcomes.
